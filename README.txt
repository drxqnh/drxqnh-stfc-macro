Important:

- Screen resolution must be set to 1920x1080
- Display scale must be set to 100% (Windows setting)
- Game must be full screen

Set up:

- Python 3.11 required
- Open command prompt and paste the following code then press enter: pip install pyautogui keyboard opencv-python numpy pynput pillow